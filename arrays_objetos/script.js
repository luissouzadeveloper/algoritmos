// arrays
let fruteira = ['Banana', 'Pêra', 'Uva'];

document.write(fruteira[1] + '<br>');

let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];

document.write(numeros + '<br>');

// objeto
let pessoa = {
    nome: 'Maria',
    idade: 60,
    programadora: false
};

document.write(pessoa.programadora + '<br>');

let carro = {
    modelo: 'HRV',
    cor: 'Prata',
    ano: '2019',
    arCondicionado: true
};
document.write(carro.modelo);
